nav
    ul 
        li 
            a(href="/").title 🔥 Firetube
        if loggedIn
            li
                a(href="/videos/upload") upload
    form(method="GET", action="/search").searchForm
        input(type="text", name="keyword")
        button(type="submit")
            i.fas.fa-magnifying-glass
    ul
        if !loggedIn 
            li 
                a(href="/join") join  
            li 
                a(href="/login") login 
            li
                a(href="/login")
                    img(src="/images/anonymous.png", width="30", height="30", style="border-radius: 70px")
        else
            li 
                a(href="/videos/record") record
            li 
                a(href="/users/logout") logout 
            li
                a(href=`/users/${loggedInUser._id}`)
                    img(src=loggedInUser.avatarUrl, width="30", height="30", style="border-radius: 70px")